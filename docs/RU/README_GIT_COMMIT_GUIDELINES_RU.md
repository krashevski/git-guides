# 📝 Соглашение о правилах коммит сообщений

[![License: CC BY-SA 4.0](https://licensebuttons.net/l/by-sa/4.0/88x31.png)](https://creativecommons.org/licenses/by-sa/4.0/)

[🇬🇧 English]( ../EN/README_GIT_COMMIT_GUIDELINES_EN.md) | [🇷🇺 Русский](README_GIT_COMMIT_GUIDELINES_RU.md)

Этот проект придерживается упрощённого варианта Conventional Commits.

## Общий формат
```bash
<type>: <short description>
```

Пример:
```bash
docs: add README for safe GitHub usage
```

## 🔖 Используемые типы коммитов

| Type        | Когда использовать                                |
| ----------- | ------------------------------------------------- |
| `feat:`     | Добавление новой функциональности                 |
| `fix:`      | Исправление ошибки                                |
| `docs:`     | Изменения в документации (README, guides)         |
| `refactor:` | Рефакторинг без изменения поведения               |
| `chore:`    | Технические изменения (скрипты, CI, cleanup)      |
| `test:`     | Добавление или изменение тестов                   |
| `build:`    | Изменения, связанные со сборкой или зависимостями |


## 📌 Примеры корректных сообщений

docs: add README_GIT_SECURITY guidelines
fix: correct symlink creation logic
feat: add i18n support for setup-symlinks
refactor: simplify disk detection logic
chore: remove obsolete test files

### ⚠️ Чего стоит избегать
❌ Плохие примеры:
```text
Update files
Fix
Changes
README update
```

Почему плохо:
- непонятно, что именно изменено
- сложно читать историю
- невозможно автоматически формировать CHANGELOG

### 🧠 Почему это важно
✔ понятная история коммитов
✔ удобно читать git log --oneline
✔ проще находить изменения
✔ в будущем можно автоматически генерировать CHANGELOG.md

### 🔍 Проверка перед коммитом
Рекомендуем перед коммитом:
```bash
git status
git diff
```

И убедиться, что:
- нет `<<<<<<<`, `=======`, `>>>>>>>`
- сообщение коммита отражает суть изменения

## 🔧 Исправить последнний коммит личного репозитария ветки main

Исправить коммит уже отправленный на GitHub:
```bash
git commit --amend -m "docs: README_GIT_GUIDES files removed"
```

Проверьте:
```bash
git log --oneline -1
```

Должно быть:
```bash
xxxxxxx docs: README_GIT_GUIDES files removed
```

Хеш изменится — это нормально.

Аккуратно обновить коммит на GitHub:
```bash
git push --force-with-lease
```

✔ безопасно
✔ не перетрёт чужие изменения
✔ корректно для личного репозитория

## 🔧 Исправить коммит и не переписывать историю (безопасно)

Если коммит уже в main и вы не хотите переписывать историю:
```bash
git add .
git commit -m "fix: исправления после релиза"
git push
```

История останется честной и понятной.

Проверка результата:
```bash
git log --oneline -3
```

## Смотри также

- Руководящие принципы по управлению репозиторием [REPOSITORY_STANDARDS_RU.md](README_REPOSITORY_STANDARDS_RU.md)
