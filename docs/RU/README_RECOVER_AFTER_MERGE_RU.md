# 📘 Восстановление файлов после неудачного git merge

[🇬🇧 English](../EN/README_RECOVER_AFTER_MERGE_EN.md) | [🇷🇺 Русский](README_RECOVER_AFTER_MERGE_RU.md)

Документ описывает безопасный и воспроизводимый порядок действий для восстановления файлов после неудачного слияния веток (git merge), в том числе в ситуациях восстановления системы или дистрибутива.

## 🎯 Цель

1. Вернуть потерянные или перезаписанные файлы.
2. Сохранить корректную историю Git.
3. Избежать --force и переписывания истории.
4. Явно зафиксировать восстановление через коммит.

## 🧠 Ключевой принцип

> [!] Ветки считаются синхронизированными только через коммиты, а не через рабочее дерево.
Команды вида:
```bash
git checkout <commit> -- <file>
```

**не меняют историю**, а только состояние файлов. Изменения становятся частью ветки **только после git commit**.

## 🟢 Сценарий 1: merge ещё не закоммичен

Проверка:
```bash
git status
```

Если Git сообщает:
```bash
You are in the middle of a merge
All conflicts fixed but you are still merging
```

➡️ Безопасный откат:
```bash
git merge --abort
```

✔ Рабочее дерево и ветка возвращаются в состояние до merge.

## 🟡 Сценарий 2: merge уже закоммичен, но файлы потеряны

### 1️⃣ Найти коммит с корректной версией файлов
```bash
git log --oneline --decorate
```

Пример:
```bash
8731eed good state before merge
```

### 2️⃣ Восстановить конкретные файлы
```bash
git checkout 8731eed -- scripts/show-system-mounts.sh
```

Повторить для всех нужных файлов.

### 3️⃣ Проверить состояние
```bash
git status
git diff
```

Убедиться, что восстановлены именно нужные изменения.

### 4️⃣ Обязательно зафиксировать восстановление
```bash
git commit -m "fix: restore scripts after failed merge"
```

✔ Теперь восстановление является частью истории ветки.

## 🔁 Синхронизация веток после восстановления

Если восстановление сделано в main, а ранее существовала ветка (например i18n):
```bash
git checkout i18n
git merge main
```

Проверка эквивалентности:
```bash
git diff main..i18n
```

Если diff пустой — ветки совпадают по содержимому.

## ❌ Чего НЕ следует делать

* git reset --hard без понимания последствий
* git push --force в main
* считать восстановление завершённым без коммита

## 🧩 Типовая причина проблемы

* восстановление дистрибутива / home каталога
* ветка (например i18n) опережала восстановленное состояние
* git merge прошёл без конфликтов, но логика была перезаписана

## ✅ Рекомендации на будущее

* локализацию (i18n) выносить в отдельные файлы
* перед merge делать:
```bash
git diff main..feature
```

* после системного восстановления всегда проверять ветки

## 📌 Итог

* Файлы можно безопасно восстановить даже после merge.
* Истина для Git — это коммиты, а не текущее состояние файлов.
* Осознанный fix-коммит — правильное завершение восстановления.

>  [I] 🛠 Документ предназначен для использования в проектах, где важны надёжность, воспроизводимость и безопасность истории Git.

## Смотри также

- Руководящие принципы по управлению репозиторием [README_REPOSITORY_STANDARDS_RU.md](README_REPOSITORY_STANDARDS_RU.md)
