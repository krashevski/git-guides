# Процесс создания запроса на слияние (Pull Request)

[🇬🇧 English](../EN/GIT_GUIDES_PULLl_REQUEST_PROCESS_EN.md) | [🇷🇺 Русский](GIT_GUIDES_PULLl_REQUEST_PROCESS_RU.md)

В этом документе описан стандартный рабочий процесс создания запроса на слияние (Pull Request, PR) для репозитория. Следование этим шагам помогает поддерживать стабильность, возможность проверки и простоту сопровождения кода.

## 1. Перед началом работы

Перед созданием запроса на слияние убедитесь, что:
- У вас есть **учетная запись GitHub** и доступ к репозиторию (fork или direct access).
- Ваш локальный репозиторий **актуален** (**up to date**) по отношению к ветке `main` (или `master`).
- Ваши изменения **ограничены** (**scoped**): один PR = одно логическое изменение.
Рекомендуется:
- Прочитайте файл `README.md` и соответствующую документацию.
- Проверьте существующие **Issues** и **Pull Request**, чтобы избежать дублирования.

## 2. Создайте архив (снимок)

Перед началом работы над значительным изменением или обновлением библиотеки:
1. Убедитесь, что ветка `main` чистая и стабильная.
2. Создайте **тег** или **архив релиза** на GitHub:
```bash
git checkout main
git pull origin main
git tag -a vX.Y.Z -m "Стабильный снимок до изменений"
git push origin vX.Y.Z
```

3. (Необязательно) Создайте релиз на GitHub на основе этого тега.
Этот архив служит точкой восстановления и эталонной базой.

## 3. Создайте ветку для новой функции

Никогда не работайте напрямую с `main`.
```bash
git checkout -b feature/short-description
```

Соглашения об именовании веток:
- `feature/...` – новая функциональность
- `fix/...` – исправления ошибок
- `docs/...` – только документация
- `refactor/...` – внутренние улучшения кода

## 4. Внесение изменений

Во время работы над вашей веткой:
- Делайте коммиты **небольшими и содержательными**.
- Пишите **четкие сообщения коммитов**:
```text
Добавьте архитектуру резервного копирования v2
Исправьте проверку разрешений для команд, доступных только root
Обновите сообщения интернационализации (JA)
```

- Запустите тесты или ручные проверки, относящиеся к вашим изменениям.

## 5. Обновление документации

Если ваши изменения влияют на поведение, конфигурацию или использование:
- Обновите или добавьте документацию в `docs/` или `Git Guides/`.
- Явно укажите новые скрипты, флаги или критические изменения.
Запросы на слияние (PR), изменяющие поведение, **должны включать обновления документации**.

## 6. Отправьте изменения в свою ветку

```bash
git push origin feature/short-description
```

## 7. Создайте запрос на слияние (Pull Request)

На GitHub:
1. Создайте запрос на слияние из своей ветки в `main`.
2. Заполните описание запроса на слияние, используя следующую структуру:

### Заголовок запроса на слияние
Краткое, описательное изложение изменений.

### Описание
- Что было изменено
- Почему это было изменено
- Какие-либо важные проектные решения

### Тип изменения
- [ ] Исправление ошибки
- [ ] Новая функция
- [ ] Рефакторинг
- [ ] Документация
- [ ] Другое

### Тестирование
- Как проводилось тестирование?
- На какой ОС/дистрибутиве?

### Контрольный список
- [ ] Код компилируется без ошибок
- [ ] Скрипты выполняются успешно
- [ ] Документация обновлена
- [ ] Внесены изменения, не относящиеся к проекту
- [ ] Никаких не имеющих отношения к проекту изменений не включено

## 8. Процесс проверки

В ходе проверки:
- Будьте открыты для обратной связи.
- Отвечайте на комментарии четко и уважительно.
- Вносите запрошенные изменения в **новые коммиты** (не переписывайте историю, если вас об этом не попросили).
При необходимости:
```bash
git commit --fixup <commit>
```

## 9. Слияние

Запрос на слияние (PR) может быть принят в следующих случаях:
- Все запрошенные изменения учтены.
- Если в репозитории настроена система непрерывной интеграции (GitHub Actions / CI), то все её проверки должны успешно выполниться.
- Получено как минимум одно одобрение (approval), если требуется правилами ветки.

Предпочтительная стратегия слияния:
- **Сжатие и слияние** для небольших запросов на слияние (сжатие всех коммитов PR в один коммит)
- **Коммит слияния** для более крупных, структурированные изменения

## 10. После слияния

После слияния запроса на слияние:
1. Удалите ветку новой функциональности (feature):
```bash
git branch -d feature/short-description
git push origin --delete feature/short-description
```

2. Загрузите обновленную ветку `main` локально.
3. (Необязательно) Создайте новый тег или релиз, если изменение значительное.


## 11. Примечания

- Крупные архитектурные изменения следует обсуждать **до** их реализации.
- Изменения, нарушающие обратную совместимость, должны быть четко обозначены и задокументированы.
- В случае сомнений, запланируйте открытие запроса на слияние заблаговременно.

Удачного программирования 🚀